#include <stdlib.h>
#include "main.h"

/**
 * _strlen - Checks the length of a line
 * @s: The character checking length
 * Return: Integer length of a line
 */

int _strlen(char *s)
{
	int i;

	while (s[i] != '\0')
